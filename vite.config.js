import { defineConfig } from "vite";

export default {
    // config options
    // root: "./src",
    root: ".",
};

// export default defineConfig({
//     root: "public", // Change this to the folder where your index.html is located
// });
